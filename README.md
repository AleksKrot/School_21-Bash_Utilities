## 🎓 Bash Utilites

Этот проект был выполнен в рамках учебного курса Школы 21 и посвящён созданию собственных реализаций базовых Bash-утилит для работы с текстом: cat и grep. Цель проекта — освоение разработки простых, но эффективных утилит на языке программирования C и приобретение практических навыков работы с терминалом Linux.

---

## 📌 Постановка задачи

В данном проекте стояли две задачи:

### Часть 1: Утилита cat
Создание собственной реализации утилиты cat, поддерживающей обработку текстовых файлов. Утилита должна уметь читать файлы и выводить их содержимое на стандартный поток вывода (stdout), обеспечивая функциональность оригинального инструмента, а также поддерживать все флаги (включая GNU версии), указанные ниже.

`cat [OPTION] [FILE]...`

| Опции | Описание |
| ------ | ------ |
| -b (GNU: --number-nonblank) | нумерует только непустые строки |
| -e предполагает и -v (GNU only: -E то же самое, но без применения -v) | также отображает символы конца строки как $  |
| -n (GNU: --number) | нумерует все выходные строки |
| -s (GNU: --squeeze-blank) | сжимает несколько смежных пустых строк |
| -t предполагает и -v (GNU: -T то же самое, но без применения -v) | также отображает табы как ^I |

### Часть 2: Утилита grep
Создание собственной реализации утилиты grep, способной искать совпадения с регулярными выражениями внутри текстовых файлов. Необходимо обеспечить совместимость утилиты с основными флагами оригинала, указанными ниже, включая их парные комбинации (например, `-iv`, `-in`).

`grep [options] template [file_name]`

| Опции | Описание |
| ------ | ------ |
| -e | Шаблон. |
| -i | Игнорирует различия регистра.  |
| -v | Инвертирует смысл поиска соответствий. |
| -c | Выводит только количество совпадающих строк. |
| -l | Выводит только совпадающие файлы.  |
| -n | Предваряет каждую строку вывода номером строки из файла ввода. |
| -h | Выводит совпадающие строки, не предваряя их именами файлов. |
| -s | Подавляет сообщения об ошибках о несуществующих или нечитаемых файлах. |
| -f file | Получает регулярные выражения из файла. |
| -o | Печатает только совпадающие (непустые) части совпавшей строки. |

---

## 🛠️ Технология и инструменты

- Язык программирования: C (стандарт C11)
- Библиотеки: Стандартные библиотеки языка C (POSIX.1-2017)
- Стиль кода: Google Style
- Средства разработки: GCC (компилятор), Makefile (для автоматизации сборки)
- Принцип разработки: В соответствии с принципами структурного программирования

---

## 🖊️ Ключевая реализация

Главная задача заключалась в изучении и воспроизведении поведения популярных Bash-утилит: cat и grep. Они были реализованы с помощью стандартных библиотек. Парсинг аргументов был осуществлен с помощью библиотеки **getopt.h**, а тестирование посредстом сравнения двух результирующих файлов: файла с результатами вызова стандартной утилиты и файла с результатом вызова собственной реализации утилиты. Сравнение производилось с помощью библиотеки **string.h**.

---

## 📌 Резюме

Эти упражнения помогли закрепить фундаментальные навыки работы с языком C и командной оболочкой Linux, а также попрактиковаться с написанием **Makefile**.

---
