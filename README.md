## 🎓 Bash Utilites

Этот проект был выполнен в рамках учебного курса Школы 21 и посвящён созданию собственных реализаций базовых Bash-утилит для работы с текстом: cat и grep. Цель проекта — освоение разработки простых, но эффективных утилит на языке программирования C и приобретение практических навыков работы с терминалом Linux.

---

## 📌 Постановка задачи

В данном проекте стояли две задачи:

### Часть 1: Утилита cat
Создание собственной реализации утилиты cat, поддерживающей обработку текстовых файлов. Утилита должна уметь читать файлы и выводить их содержимое на стандартный поток вывода (stdout), обеспечивая функциональность оригинального инструмента, а также поддерживать все флаги (включая GNU версии), указанные ниже.

`cat [OPTION] [FILE]...`

| № | Опции | Описание |
| ------ | ------ | ------ |
| 1 | -b (GNU: --number-nonblank) | нумерует только непустые строки |
| 2 | -e предполагает и -v (GNU only: -E то же самое, но без применения -v) | также отображает символы конца строки как $  |
| 3 | -n (GNU: --number) | нумерует все выходные строки |
| 4 | -s (GNU: --squeeze-blank) | сжимает несколько смежных пустых строк |
| 5 | -t предполагает и -v (GNU: -T то же самое, но без применения -v) | также отображает табы как ^I |

### Часть 2: Утилита grep
Создание собственной реализации утилиты grep, способной искать совпадения с регулярными выражениями внутри текстовых файлов. Необходимо обеспечить совместимость утилиты с основными флагами оригинала, такими как -e, -i, -v, -c, -l, -n.

---

## 🛠️ Технология и инструменты

- Язык программирования: C (стандарт C11)
- Библиотеки: Стандартные библиотеки языка C (POSIX.1-2017)
- Стиль кода: Google Style
- Средства разработки: GCC (компилятор), Makefile (для автоматизации сборки)
- Принцип разработки: В соответствии с принципами структурного программирования

---

## 🖊️ Ключевая реализация

Ваша главная задача заключалась в изучении и воспроизведении поведения популярных Bash-утилит. Например, реализуя команду cat, вы познакомились с методами чтения и вывода файлов, а работая над grep, изучали методы сопоставления регулярных выражений и парсинга аргументов командной строки.

---

## 📌 Резюме

Эти упражнения помогли закрепить фундаментальные навыки работы с языком C и командной оболочкой Linux. Теперь вы готовы создавать собственные небольшие инструменты, которые могут существенно упростить рутинные операции в повседневной работе.

---
